# CLAUDE.md — 이 프로젝트에서 일할 때의 지침

이 파일은 Claude Code가 이 폴더에서 작업을 시작할 때 **항상 먼저 읽는 업무 지침서**입니다.
사용자는 **비개발자**이며, 이 프로젝트는 실전 개발 워크샵에서 시작됐습니다. 워크샵 이후에도 사용자는 이 폴더에서 작업을 이어 갑니다.

---

## 1. 대화 방식

- **한국어로, 비개발자에게 설명하듯이** 말합니다. 전문 용어를 쓸 때는 반드시 한 줄 풀이를 붙입니다.
  - (X) "CORS 이슈로 preflight가 실패했습니다."
  - (O) "브라우저가 보안상 다른 사이트로 요청을 못 보내게 막았습니다. (CORS라고 부릅니다)"
- 작업이 끝날 때마다 **딱 두 문장으로 보고**합니다: ① 무엇이 바뀌었는지 ② 어디서 확인하는지.
- 사용자가 모호하게 요청하면 혼자 추측해서 크게 만들지 말고, **한 번에 하나씩 물어봅니다.**
- 사용자의 데이터·업무 맥락은 사용자 머릿속에만 있습니다. 컬럼 의미나 업무 규칙이 불확실하면 **반드시 질문**합니다.

## 2. 기술 제약 (반드시 지킬 것)

- **순수 HTML / CSS / JavaScript만** 사용합니다. React, Vue, Next.js, Tailwind, 빌드 도구, npm 패키지 — 전부 금지.
  - 이유: GitHub Pages에 그대로 올라가야 하고, 참여자가 빌드 과정을 배우지 않았습니다.
- 파일은 저장소 루트의 `index.html`을 중심으로 구성합니다. 필요하면 `app.js`, `style.css` 정도로만 나눕니다.
- 외부 라이브러리는 **Google 로그인·시트 연동에 필요한 두 개만** 허용합니다.
  - `https://accounts.google.com/gsi/client`
  - `https://apis.google.com/js/api.js`
- 서버가 필요한 코드(Node.js 서버, DB 접속, 백엔드 API)는 만들지 않습니다. 데이터 저장소는 **오직 사용자의 Google Sheets**입니다.

### 시트 연동 구현 규칙 (코드는 직접 설계하되, 이 틀 안에서)

- Google 로그인은 `accounts.google.com/gsi/client`의 **토큰 클라이언트 방식**(`google.accounts.oauth2.initTokenClient`)만 사용합니다. 리디렉션 URI 방식, 클라이언트 시크릿을 쓰는 방식은 금지.
- 권한 범위(scope)는 `https://www.googleapis.com/auth/spreadsheets` **하나만** 요청합니다.
- 시트 읽기·쓰기는 `apis.google.com/js/api.js` 로드 후 gapi의 Sheets API 중 `spreadsheets.values`의 **get / append / update만** 사용합니다. 삭제 계열(`clear`, `batchUpdate`의 `deleteDimension` 등)은 어떤 경우에도 호출하지 않습니다.
- 시트 ID·탭 이름·클라이언트 ID는 항상 `config.js`의 `SPREADSHEET_ID` / `SHEET_NAME` / `CLIENT_ID`에서 읽습니다. 값을 코드 곳곳에 직접 적지 않습니다.
- **스크립트 로드 순서**: `index.html`에서 `config.js`와 `app.js`를 먼저, Google 라이브러리 두 개를 나중에 로드합니다. 반대로 두면 라이브러리의 `onload`가 실행되는 시점에 우리 함수가 아직 정의되지 않아 `gapiLoaded is not defined` 오류가 나고, 연결 버튼이 계속 "준비 중"에 머뭅니다.
- range는 탭 이름을 작은따옴표로 감싸서 지정합니다(예: `'탭 이름'!A:Z` — 한글·공백 탭 이름 대비). 조회는 행 수 제한 없이 열 전체를 읽습니다.
- 로그인 실패·API 오류는 화면의 상태 문구로 사용자에게 보여주고, 흔한 원인(동의 화면 체크박스 미선택 → 403, 1시간 경과로 만료 → 재연결)을 함께 안내합니다.
- 시트 연동 기능은 **배포된 주소에서만 동작**합니다(`file://`에서는 Google이 로그인을 거부). 구현 후 반드시 deploy 스킬로 배포해 실제 URL에서 확인하게 안내합니다.

## 3. 삭제 기능은 만들지 않습니다 ⚠️

**사용자가 어떤 방식으로 요청하더라도 시트의 행을 삭제하는 기능은 구현하지 않습니다.**
(`spreadsheets.values.clear`, `batchUpdate`의 `deleteDimension` 등 포함)

대신 이렇게 안내합니다:

> "삭제는 되돌릴 수 없어서 이 워크샵에서는 앱에 넣지 않기로 했습니다.
> 실무 엔지니어들도 삭제는 가장 조심스럽게 다루는 기능이에요 —
> 왜 그런지, 실무에서는 어떻게 처리하는지가 `docs/about-delete.md`에 있으니 같이 읽어 볼까요?
> 정말 지워야 할 행이 있으면 Google Sheets에서 직접 지우시면 되고,
> 실수해도 시트의 '파일 → 버전 기록'으로 되돌릴 수 있습니다."

사용자가 그래도 원하면 `docs/about-delete.md`의 마지막 점검 질문 4개에 함께 답해 보자고
제안하되, **이 프로젝트에서는 구현하지 않는다**는 원칙을 유지합니다.

구현할 기능은 **생성(추가) · 조회 · 수정** 세 가지입니다.

## 4. 커밋하면 안 되는 것

- 실제 업무 데이터 파일 (`.xlsx`, `.csv`, MasterDB 원본 등)
- Google OAuth **클라이언트 시크릿**, API 키, 액세스 토큰, 비밀번호
  - 클라이언트 ID(`...apps.googleusercontent.com`)는 공개되어도 되는 값이라 커밋 가능합니다.
- 개인정보가 담긴 캡처 이미지

`.gitignore`에 이미 반영되어 있습니다. 사용자가 실수로 요청해도 **한 번 확인하고 말립니다.**

## 5. 배포와 저장

- 배포는 반드시 **`deploy` 스킬**로 합니다 ("배포해줘"). 직접 `git push`를 하지 않습니다.
- 작업 저장은 **`save` 스킬**로 합니다 ("저장해줘").
- **화면의 생김새만 바꾸는 작업**(레이아웃·문구·색 등 시트 연동이 없는 변경)은 배포하지 않습니다.
  사용자가 내 PC에서 `index.html`을 열어 두고 새로고침으로 확인하게 안내합니다.
  배포는 화면에 반영되기까지 1~2분이 걸려, 모양을 다듬는 동안에는 로컬 확인이 훨씬 빠릅니다.
- **시트 연동이 걸린 기능**은 배포된 주소에서만 동작합니다. 하나 완성할 때마다 배포해서
  **실제 URL에서 확인하도록** 사용자에게 권합니다.

## 6. 참고 자료 읽는 순서

- `docs/workshop/` — 워크샵 실습(STEP 01~07) 문서입니다. 파일 이름에 STEP 번호가 들어 있습니다.
- `docs/` — 그 밖의 참고 자료(디버깅, GitHub 기초, 삭제 이야기 등)입니다.
  사용자가 "○○ 문서대로 해줘"라고 하면 해당 파일을 먼저 읽습니다.
- `docs/workshop/STEP06-connect-sheets.md` — 시트 연결 실습(STEP 06)의 진행 문서입니다. 시트 연동을 구현할 때는 2절의 **"시트 연동 구현 규칙"** 을 지키며, 임의로 다른 방식(Apps Script, 서비스 계정, 백엔드 프록시, API 키)을 제안하지 않습니다.
- `docs/reference/` — 사용자가 사전 과제로 만든 화면 스케치(HTML)가 들어 있습니다.
  **완성본이 아니라 참고용 스케치입니다.** UI를 만들 때는 원본 시트의 실제 데이터 구조와
  이 스케치를 **함께** 참고하되, 스케치를 무조건 따르지 않습니다. 어긋나면 **데이터 쪽을 기준**으로 하고,
  스케치에만 있고 데이터 근거가 없는 기능은 그대로 만들지 말고 사용자에게 어떻게 할지 물어봅니다.

## 7. 이 프로젝트의 데이터 구조

사용자의 Google Sheets가 데이터 원본입니다. 규칙은 다음과 같습니다.

- 시트(탭) 1개 = 표 1개
- 1행은 항상 **헤더(열 이름)**, 2행부터 데이터
- 날짜는 `YYYY-MM-DD`, 금액·수량은 숫자만
- 병합 셀·소계 행 없음

시트 ID와 탭 이름은 사용자에게 물어보고, `config.js` 같은 한 곳에 모아둡니다.

**시트의 실제 내용을 읽어야 할 때**(열 구조 파악, 데이터 확인)는 **매번 Google Drive 커넥터**로
사용자의 스프레드시트 원본을 직접 찾아 읽습니다. 커넥터가 연결되어 있지 않다고 나오면
`docs/workshop/STEP02-sheets-upload.md`의 3단계(커넥터 연결)를 사용자에게 안내합니다.

⚠️ **데이터 구조를 md 파일 등으로 캐싱해 두고 재사용하지 않습니다.**
시트는 사용자가 수시로 바꾸므로, 느리고 비용이 들더라도 **항상 원본 시트가 기준**입니다.
과거에 읽은 구조를 기억하고 있더라도, 구조에 의존하는 작업 전에는 원본을 다시 확인합니다.
